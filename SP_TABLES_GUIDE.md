# Работа с таблицами в СП

## Обзор

Система поддерживает создание и управление нормативными таблицами для пунктов СП с расширенной функциональностью:

- ✅ **Ручной конструктор** - создание таблиц через интерфейс
- ✅ **AI-генератор** - автоматическое создание таблиц через описание
- ✅ **Примечания** - добавление заметок к таблицам (markdown поддерживается)
- ✅ **Объединение ячеек** - rowspan/colspan для сложных таблиц
- ✅ **Публичный просмотр** - автоматическое отображение таблиц на странице пункта СП

## Структура данных таблицы

```typescript
{
  title: string;              // Название таблицы
  description?: string;       // Описание
  notes?: string;            // Примечания (markdown)
  columns: [{                // Определение колонок
    name: string;            // Имя (латиница)
    label: string;           // Отображаемое название
    type: "text" | "number"; // Тип данных
    unit?: string;           // Единица измерения
  }];
  rows: (string | number)[][]; // Данные таблицы
  cell_meta?: {              // Метаданные ячеек для объединения
    "row,col": {
      rowspan?: number;      // Объединение по вертикали
      colspan?: number;      // Объединение по горизонтали
      hidden?: boolean;      // Ячейка скрыта (покрыта объединением)
    }
  }
}
```

## Использование

### 1. Ручное создание таблицы

**Где:** `/admin/sp/[code]` → Раздел TableManager для каждого пункта

**Шаги:**
1. Нажать **"+ Новая таблица"**
2. Заполнить:
   - **Название** - обязательное поле
   - **Описание** - опционально
   - **Примечания** - опционально, поддерживает markdown
3. Настроить колонки:
   - Добавить колонки кнопкой **"+ Добавить колонку"**
   - Для каждой колонки указать:
     - Название
     - Тип (текст/число)
     - Единицу измерения (опционально)
4. Заполнить данные:
   - Добавить строки кнопкой **"+ Добавить строку"**
   - Ввести значения в ячейки
5. Нажать **"Сохранить"**

**Ограничения:**
- Минимум 1 колонка
- Минимум 1 строка
- Объединение ячеек доступно только через AI-генератор

### 2. AI-генератор таблиц

**Где:** `/admin/sp/[code]` → Нажать **"🤖 AI генератор"**

**Шаги:**
1. Описать таблицу на русском языке, например:
   ```
   Создай таблицу коэффициентов снеговой нагрузки для 5 регионов России.
   Колонки: регион, базовое значение (кПа), коэффициент надежности.
   Добавь примечание о применении для особых случаев.
   ```

2. Нажать **"✨ Сгенерировать таблицу"**

3. Просмотреть результат в превью

4. Нажать **"Сохранить таблицу"** для добавления в базу

**Возможности AI:**
- Создание колонок с правильными типами и единицами
- Генерация реалистичных данных
- Объединение ячеек (rowspan/colspan) при необходимости
- Автоматическое добавление примечаний

**Примеры запросов:**
```
Таблица классов бетона с характеристиками прочности

Коэффициенты условий работы для стали при разных температурах

Предельные прогибы для балок и плит перекрытий

Таблица с объединенными ячейками: типы нагрузок (постоянные и временные)
и их коэффициенты надежности
```

### 3. Просмотр таблиц на сайте

Таблицы автоматически отображаются на публичной странице пункта СП:

**Где:** `/sp/[code]?clause=[id]`

**Раздел:** "Нормативные таблицы" (между формулами и калькуляторами)

**Отображение:**
- Заголовок таблицы и описание
- Колонки с единицами измерения
- Данные с форматированием (числа с разделителями)
- Объединенные ячейки (если заданы)
- Примечания внизу таблицы

## База данных

### Миграции

**0006_sp_tables.sql** - Основная схема:
- Таблица `sp_tables`
- Базовые поля (title, description, columns, rows)
- RLS политики (публичный просмотр, редактирование только founders)

**0007_sp_tables_extended.sql** - Расширения (НОВОЕ):
- Поле `notes` - примечания к таблице
- Поле `cell_meta` - метаданные для объединения ячеек

### Применение миграций

**Через Supabase Dashboard:**
1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. SQL Editor → New Query
3. Скопируйте содержимое `0007_sp_tables_extended.sql`
4. Нажмите Run (F5)

**Через скрипт:**
```bash
node scripts/run-migration.mjs
```

## API

### Endpoints

**GET /api/sp-tables**
- Получение списка таблиц
- Query: `?spCode=СП20&clauseId=5.2`
- Публичный доступ к активным таблицам

**POST /api/sp-tables**
- Создание новой таблицы
- Требует авторизации founder
- Поддерживает notes и cell_meta

**PUT /api/sp-tables?id=**
- Обновление таблицы
- Требует авторизации founder

**DELETE /api/sp-tables?id=**
- Soft delete (is_active = false)
- Требует авторизации founder

**POST /api/sp-tables/generate** (НОВОЕ)
- AI-генерация таблицы
- Body: `{ prompt: "описание таблицы" }`
- Требует авторизации founder
- Использует DeepSeek API

## Примеры объединения ячеек

```typescript
// Простое объединение по вертикали (2 строки)
cell_meta: {
  "0,0": { rowspan: 2 },     // Ячейка на 2 строки
  "1,0": { hidden: true }     // Ячейка скрыта
}

// Объединение по горизонтали (3 колонки)
cell_meta: {
  "0,0": { colspan: 3 },     // Ячейка на 3 колонки
  "0,1": { hidden: true },    // Скрыта
  "0,2": { hidden: true }     // Скрыта
}

// Комбинированное (2x2)
cell_meta: {
  "0,0": { rowspan: 2, colspan: 2 },
  "0,1": { hidden: true },
  "1,0": { hidden: true },
  "1,1": { hidden: true }
}
```

## Планы на будущее

- [ ] **Ручной редактор объединения ячеек** - UI для настройки rowspan/colspan
- [ ] **Ссылки на таблицы** - `{{table:id}}` для вставки в текст
- [ ] **Ссылки на строки** - `{{table:id:row:2}}` для ссылки на конкретную строку
- [ ] **Использование данных в расчетах** - автоматическая подстановка значений из таблиц в формулы
- [ ] **Экспорт таблиц** - в Excel, CSV, JSON
- [ ] **Импорт таблиц** - из Excel, CSV
- [ ] **Версионирование** - история изменений таблиц
- [ ] **Поиск по таблицам** - полнотекстовый поиск
- [ ] **Фильтрация и сортировка** - интерактивные таблицы на фронте

## Технические детали

**Компоненты:**
- `AITableGenerator.tsx` - AI-генератор таблиц
- `TableEditor.tsx` - Ручной редактор таблиц
- `TableManager.tsx` - Управление таблицами в админке
- `SpTableView.tsx` - Публичный просмотр таблиц

**API Routes:**
- `app/api/sp-tables/route.ts` - CRUD операции
- `app/api/sp-tables/generate/route.ts` - AI генерация

**Типы:**
```typescript
type SpTableColumn = {
  name: string;
  label: string;
  type: "text" | "number";
  unit?: string;
};

type CellMeta = {
  rowspan?: number;
  colspan?: number;
  hidden?: boolean;
};

type SpTableRecord = {
  id: string;
  sp_code: string;
  clause_id: string;
  title: string;
  description?: string;
  notes?: string;
  columns: SpTableColumn[];
  rows: (string | number)[][];
  cell_meta?: Record<string, CellMeta>;
  created_by: string | null;
  created_at: string;
  updated_at: string;
  is_active: boolean;
};
```

## Примеры использования

### Пример 1: Простая таблица

```typescript
{
  title: "Классы бетона",
  columns: [
    { name: "class", label: "Класс", type: "text" },
    { name: "rb", label: "Rb", type: "number", unit: "МПа" }
  ],
  rows: [
    ["B15", 8.5],
    ["B20", 11.5],
    ["B25", 14.5]
  ]
}
```

### Пример 2: Таблица с примечаниями

```typescript
{
  title: "Коэффициенты надежности",
  columns: [...],
  rows: [...],
  notes: "¹⁾ Для особых сочетаний коэффициенты снижаются на 0.1\n²⁾ Применяется при расчете по предельным состояниям"
}
```

### Пример 3: Таблица с объединенными ячейками

```typescript
{
  title: "Типы нагрузок",
  columns: [
    { name: "type", label: "Тип", type: "text" },
    { name: "perm", label: "Постоянная", type: "number" },
    { name: "temp", label: "Временная", type: "number" }
  ],
  rows: [
    ["Конструкции", 1.1, 1.2],
    ["Металл", 1.05, 1.2],
    ["Снег", 1.1, 1.4]
  ],
  cell_meta: {
    "0,0": { rowspan: 2 },
    "1,0": { hidden: true }
  }
}
```
