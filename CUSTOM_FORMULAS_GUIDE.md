# 📐 Визуальный Конструктор Формул

## Обзор

Теперь админы могут создавать кастомные формулы без необходимости менять код. Через удобный интерфейс можно:

1. **Определить название и описание** формулы
2. **Написать LaTeX** представление формулы (для красивого отображения)
3. **Добавить параметры** с названиями, единицами измерения и диапазонами значений
4. **Написать выражение расчёта** на JavaScript
5. **Протестировать** формулу перед сохранением
6. **Сохранить** в БД для использования на сайте

## Где находится

Страница: `/admin/sp/[code]`

Каждое СП (строительное правило) имеет свой набор кастомных формул.

## Как создать формулу

### 1. Откройте админ-панель
```
/admin/sp/СП296.1325800.2017
```

### 2. Найдите раздел "Кастомные формулы СП"

### 3. Нажмите "+ Новая формула"

### 4. Заполните форму

#### Название
Короткое описание формулы, например: "Расчёт напряжения при сжатии"

#### Описание (опционально)
Подробное объяснение, когда и как её использовать

#### LaTeX формула
Математическое представление для красивого отображения.

**Примеры:**
```latex
\sigma = \dfrac{N}{A}
M = F \times d
A = \pi r^2
```

#### Параметры

Добавьте каждый параметр через "+ Параметр":

- **Имя параметра** (name): `N` или `Force` - используется в выражении
- **Отображаемое имя** (label): "Осевая сила" - видно пользователю
- **Единица** (unit): "кН", "мм²", "МПа" - опционально
- **Min/Max** - допустимые диапазоны значений

**Пример:**
```
N (Осевая сила) | кН | min: 0, max: 10000
A (Площадь) | мм² | min: 1, max: 100000
```

#### Выражение расчёта

Напишите JavaScript выражение, которое вычислит результат.

**Доступные переменные:**
- `values.ПАРАМЕТР_NAME` - значения параметров
- `Math` - все функции JavaScript Math

**Примеры:**
```javascript
values.N / values.A
Math.sqrt(values.x * values.y)
values.Force * values.distance / 1000
Math.PI * values.radius ** 2
```

#### Название результата
По умолчанию "Результат", можно переопределить

#### Единица результата
"МПа", "мм", "кВт" и т.д.

### 5. Протестируйте формулу

В синей секции "Тест формулы":

1. Введите значения параметров
2. Нажмите "Протестировать"
3. Увидите результат или ошибку

**Важно:** Если выражение синтаксически недопустимо или возвращает не-число, вы сразу узнаете об этом.

### 6. Сохраните формулу

Нажмите "Создать формулу" - она сохранится в БД и будет доступна на сайте.

## Примеры Формул

### Формула 1: Прямоугольное сечение

```
Название: Площадь прямоугольного сечения
LaTeX: A = b \\times h
Параметры:
  - b (Ширина) | мм | min: 1, max: 10000
  - h (Высота) | мм | min: 1, max: 10000
Выражение: values.b * values.h
Результат: "Площадь сечения" | мм²
```

### Формула 2: Круговое сечение

```
Название: Площадь кругового сечения
LaTeX: A = \\pi r^2
Параметры:
  - r (Радиус) | мм | min: 0.1, max: 10000
Выражение: Math.PI * values.r * values.r
Результат: "Площадь" | мм²
```

### Формула 3: Напряжение сжатия

```
Название: Напряжение при осевом сжатии
LaTeX: \\sigma_c = \\dfrac{N}{A}
Параметры:
  - N (Сила сжатия) | кН | min: 0
  - A (Площадь) | мм² | min: 1
Выражение: (values.N * 1000) / values.A
Результат: "σ_c" | МПа
```

## Как используются формулы на сайте

1. **На странице просмотра пункта** (`/sp/[code]?clause=5.2`)
   - Пользователь видит список доступных формул
   - Может ввести параметры
   - Формула рассчитывается в реальном времени

2. **В расчётах** - формулы отображаются с интерактивными полями ввода

## Безопасность

✅ **Защита выражений**
- Выражения выполняются в изолированной среде с ограниченным доступом
- Доступны только `values` и `Math`, ничего больше
- Нет доступа к файловой системе, сети или другим функциям

✅ **Права доступа**
- Только пользователи с ролью **founder** могут создавать формулы
- Каждый создатель может редактировать/удалять только свои формулы

✅ **Валидация**
- Все входные данные проверяются на корректность
- Выражение проверяется перед сохранением
- Результат должен быть числом

## Миграция БД

Перед использованием конструктора нужно применить миграцию:

```bash
node scripts/run-migration.mjs
```

Или вручную через Supabase Dashboard:
1. SQL Editor
2. Вставить содержимое `supabase/migrations/0005_custom_formula_blocks.sql`
3. Run

## Архитектура

```
Frontend (React):
  - /admin/sp/[code] → CustomFormulaManager
  - CustomFormulaManager → FormulaBuilder
  - FormulaBuilder → ParamEditor
  ↓
API Routes:
  - POST /api/custom-formulas/[spCode] → создание
  - PUT /api/custom-formulas/[id] → редактирование
  - DELETE /api/custom-formulas/[id] → удаление
  - POST /api/custom-formulas/test → тестирование выражения
  - GET /api/custom-formulas/[spCode] → загрузка списка
  ↓
Supabase:
  - Таблица: custom_formula_blocks
  - RLS policies: только авторизованные founders
  ↓
Frontend (Public):
  - /sp/[code] → SpClauseView
  - Загружает кастомные формулы
  - FormulaBlockView отображает и рассчитывает

API /api/formulas/compute:
  - Поддерживает как встроенные, так и кастомные формулы
  - Параметр spCode нужен для загрузки кастомных
```

## Ограничения и Пределы

- **Максимум параметров:** 20 на формулу
- **Максимальная длина выражения:** 10000 символов
- **Максимальная длина LaTeX:** 2000 символов
- **Получение:** 1 запрос за раз на 250ms (debounced)

## Поиск и устранение неисправностей

### "Expression must return a number"
- Ваше выражение возвращает `undefined`, `null` или строку
- Проверьте, что все переменные используются правильно: `values.N` (не `values.n`)

### "Unknown formula"
- Формула не загружена на сервер
- Проверьте, что вы сохранили новую формулу
- Может потребоваться обновление страницы

### "Invalid expression syntax"
- Синтаксическая ошибка в JavaScript
- Проверьте скобки и операторы
- Используйте правильный синтаксис: `values.a` не `values['a']`

### "Forbidden: only founders can create formulas"
- У вас нет прав создавать формулы
- Контактируйте администратора проекта

## Примечания

- Формулы хранятся в базе данных полностью независимо от кода
- Обновление конструктора не затрагивает существующие формулы
- Можно редактировать формулы без перезагрузки приложения
- Удаление формулы - это soft delete (помечается как неактивная)
